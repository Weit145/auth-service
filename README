## 🔐 Auth Service

**Auth Service** — микросервис аутентификации и авторизации в составе микросервисной платформы.

Сервис отвечает за регистрацию пользователей, аутентификацию, валидацию токенов и управление JWT.  
Auth Service предоставляет gRPC-интерфейс и используется другими внутренними сервисами системы, включая Gateway и User Service. Прямой внешний доступ к сервису отсутствует.

---

### 🔧 Зона ответственности

- регистрация пользователей;
- аутентификация (логин);
- генерация и валидация JWT;
- проверка прав доступа;
- предоставление gRPC API для внутренних сервисов;
- взаимодействие с базой данных пользователей.

---

### 🚀 Технологический стек

- **Python 3.11+**
- **FastAPI** — вспомогательные HTTP-компоненты (healthcheck, утилиты)
- **gRPC** — межсервисное взаимодействие
- **PostgreSQL 16**
- **SQLAlchemy (async)** — ORM для работы с базой данных
- **asyncpg** — асинхронный драйвер PostgreSQL
- **Poetry** — управление зависимостями и окружением
- **Docker** — контейнеризация и деплой

---

### 🧩 Взаимодействие в системе

- принимает gRPC-запросы от:
  - API Gateway;
  - User Service;
  - других внутренних микросервисов;
- использует собственную изолированную базу данных (`postgres_auth`);
- не имеет прямого HTTP-доступа извне.

---

### 🛡️ Примечания

- вся логика безопасности централизована в Auth Service;
- JWT используются как основной механизм авторизации;
- gRPC-контракты определяются в каталоге `proto`;
- сервис масштабируется независимо от остальных компонентов системы.

---
